model:
  path: models/full_pipeline.pkl
  version: 1.0.0

confidence:
  base_weight: 0.7
  shap_weight: 0.2
  retrieval_weight: 0.1

decision_modes:
  safe_threshold: 0.30
  verbose_threshold: 0.65

logging:
  level: INFO

